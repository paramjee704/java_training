1.1 // java program to swap two numbers
public class Main 
{
    public static void main(String[] args) 
    {
        int t;
        int x = 100;
        int y = 200;
         
        //Swapping in steps
        t = x;
        x = y;
        y = t;
        System.out.println("x = " + x + " and y = " + y);
    }
}

//java program swap two numbers without third variable
public class Main 
{
    public static void main(String[] args) 
    {
        int x = 100;
        int y = 200;
 
        //Swapping in steps
        x = x + y;      //x = 100 + 200 = 300
        y = x - y;      //y = 300 - 200 = 100
        x = x - y;      //x = 300 - 100 = 200
        System.out.println("x = " + x + " and y = " + y);
    }
}

1.2 // Write a program to print the value given by the user.

import java.util.Scanner;

public class Getinput {
    public static void main(String args[])
    {
        String s;
        Scanner obj = new Scanner(System.in);           /* create a object */

        System.out.println("Enter a string:");

        s = obj.nextLine();                             /* Take string input and assign to variable */

        System.out.println("You entered string "+s);    /* Print */
    }


}
  

1.3 //Write a program to complete the task given below:
Ask the user to enter any 2 numbers in between 1-10 and add both of them to another variable call z.
Use z for adding 30 into it and print the final result by using variable results.



import java.util.Scanner;

public class Addnumbers {
    public static void main(String args[])
    {
        int x, y, z;

        System.out.println("Enter two integers and add their sum");
        Scanner in = new Scanner(System.in);

        x = in.nextInt();
        y = in.nextInt();
        z = x + y;

        System.out.println("Sum of the integers = " + z);

        int result= z + 30;
        System.out.println("Final result:" + result);
    }
}

   
